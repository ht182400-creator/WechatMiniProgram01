# 08_开发过程FAQ_更新02

> 股票量化分析与预测系统 - 开发过程中常见问题汇总（第二次更新）

---

## 问题汇总列表

| 序号 | 问题编号 | 问题描述 | 日期时间 | 状态 |
|------|----------|----------|----------|------|
| 1 | FAQ-021 | POST /api/backtest/run 返回 422 Unprocessable Content | 2026-05-19 23:38 | ✅ 已解决 |
| 2 | FAQ-022 | getHistory(10) 报 TypeError: target must be an object | 2026-05-19 23:39 | ✅ 已解决 |
| 3 | FAQ-023 | 回测执行返回 500 Internal Server Error | 2026-05-19 23:42 | ✅ 已解决 |
| 4 | FAQ-024 | 日期显示为 "272" 而非有效日期字符串 | 2026-05-19 23:44 | ✅ 已解决 |
| 5 | FAQ-025 | 权益曲线图不显示 | 2026-05-19 23:46 | ✅ 已解决 |
| 6 | FAQ-026 | 策略名称显示为英文，需改为中文+英文 | 2026-05-19 23:47 | ✅ 已解决 |
| 7 | FAQ-027 | 回测结果需显示交易买卖明细 | 2026-05-19 23:56 | ✅ 已解决 |

---

## 详细内容

---

### FAQ-021: POST /api/backtest/run 返回 422 Unprocessable Content

**发生时间：** 2026-05-19 23:38
**问题类型：** API 请求参数验证错误
**影响范围：** 回测功能

---

#### 问题描述

前端调用 `backtestApi.run()` 发送 POST 请求时报错：

```
POST http://localhost:5173/api/backtest/run 422 (Unprocessable Content)
```

---

#### 错误分析

后端 `/api/backtest/run` 接口使用 Query 参数接收请求：

```python
@router.post("/run", summary="运行回测")
async def run_backtest(
    code: str = Query(...),
    strategy_name: str = Query(...),
    ...
)
```

而前端使用 Axios 发送 JSON 请求体，导致参数无法正确解析。

---

#### 解决方案

将 Query 参数改为 Pydantic BaseModel 接收 JSON 请求体：

```python
class BacktestRequest(BaseModel):
    """回测请求参数"""
    code: str
    strategy_name: str
    start_date: Optional[str] = None
    end_date: Optional[str] = None
    initial_cash: float = 100000
    params: Optional[List[Dict[str, float]]] = None

@router.post("/run", summary="运行回测")
async def run_backtest(request: BacktestRequest) -> Dict[str, Any]:
    ...
```

**修改文件：** `backend/api/backtest.py`

---

### FAQ-022: getHistory(10) 报 TypeError: target must be an object

**发生时间：** 2026-05-19 23:39
**问题类型：** JavaScript 类型错误
**影响范围：** 回测历史加载

---

#### 问题描述

```javascript
TypeError: target must be an object
    at loadHistory (Backtest.vue:152:17)
```

---

#### 错误分析

前端传递了错误的参数类型：

```javascript
// 错误写法
const res = await backtestApi.getHistory(10)

// Axios 期望对象参数
getHistory(params: { limit: number })
```

---

#### 解决方案

修改为传递对象参数：

```javascript
// 正确写法
const res = await backtestApi.getHistory({ limit: 10 })
```

**修改文件：** `frontend/web/src/pages/Backtest.vue`

---

### FAQ-023: 回测执行返回 500 Internal Server Error

**发生时间：** 2026-05-19 23:42
**问题类型：** 后端回测引擎异常
**影响范围：** 回测功能

---

#### 问题描述

```
POST http://localhost:5173/api/backtest/run 500 (Internal Server Error)
```

---

#### 错误分析

后端缺少详细的错误日志，难以定位问题。需要添加 try-catch 和详细的 logging。

---

#### 解决方案

在 `backend/api/backtest.py` 中添加详细的错误处理和日志记录：

```python
try:
    # 回测逻辑
    result = engine.run()
except Exception as e:
    logger.error(f"回测执行失败: {e}")
    raise HTTPException(status_code=500, detail=f"回测执行失败: {str(e)}")
```

---

### FAQ-024: 日期显示为 "272" 而非有效日期字符串

**发生时间：** 2026-05-19 23:44
**问题类型：** DataFrame 索引问题
**影响范围：** 所有策略生成的交易信号

---

#### 问题描述

错误日志显示：
```
[ERROR] 回测执行失败: Given date string "272" not likely a datetime, at position 0
```

权益曲线图表中 X 轴显示 "272" 而非有效日期。

---

#### 错误分析

在回测引擎执行 `reset_index(drop=True)` 后，DataFrame 的索引从 datetime 类型变成了整数 (0, 1, 2...)。

代码中试图从索引获取日期：
```python
# 错误：索引此时是整数 0, 1, 2...
date_str = str(idx.date())
```

---

#### 解决方案

所有策略文件中，从 `date` 列获取日期而非依赖索引：

```python
for idx, row in df.iterrows():
    # 从 date 列获取日期，而非依赖索引
    date_val = row['date']
    if hasattr(date_val, 'strftime'):
        date_str = date_val.strftime('%Y-%m-%d')
    else:
        date_str = str(date_val)
```

**修改文件：**
- `backend/strategies/ma_cross_strategy.py`
- `backend/strategies/rsi_strategy.py`
- `backend/strategies/macd_strategy.py`
- `backend/strategies/bollinger_strategy.py`
- `backend/engine/backtest_engine.py`

---

### FAQ-025: 权益曲线图不显示

**发生时间：** 2026-05-19 23:46
**问题类型：** 数据稀疏
**影响范围：** 回测结果图表

---

#### 问题描述

回测结果显示了统计数据，但权益曲线图表为空白。

---

#### 错误分析

`_execute_signal` 方法仅在 `SignalType.HOLD` 时记录权益曲线，导致数据点过于稀疏。

---

#### 解决方案

修改为每次执行信号时都记录权益曲线：

```python
def _execute_signal(self, signal: TradingSignal) -> None:
    """执行交易信号"""
    # 每次都记录权益曲线（原来只在 HOLD 时记录，数据太稀疏）
    self._record_equity(signal.date)
    
    if signal.signal == SignalType.HOLD:
        return
    # ... BUY/SELL logic
```

**修改文件：** `backend/engine/backtest_engine.py`

---

### FAQ-026: 策略名称显示为英文，需改为中文+英文

**发生时间：** 2026-05-19 23:47
**问题类型：** 显示优化
**影响范围：** 回测策略选择和结果显示

---

#### 问题描述

策略名称显示为英文（如 "MACrossStrategy"），需要显示为中文+英文格式。

---

#### 解决方案

**1. 修改策略类 `name` 属性：**

| 文件 | 旧 name | 新 name |
|------|---------|---------|
| `ma_cross_strategy.py` | "MACrossStrategy" | "均线交叉 (MA)" |
| `rsi_strategy.py` | "RSIStrategy" | "RSI超买超卖 (RSI)" |
| `macd_strategy.py` | "MACDStrategy" | "MACD指标 (MACD)" |
| `bollinger_strategy.py` | "BollingerStrategy" | "布林带 (BOLL)" |

**2. 前端添加策略名称映射：**

```javascript
const strategyNameMap = {
  'ma_cross': '均线交叉 (MA)',
  'MACrossStrategy': '均线交叉 (MA)',
  'rsi': 'RSI超买超卖 (RSI)',
  'RSIStrategy': 'RSI超买超卖 (RSI)',
  'macd': 'MACD指标 (MACD)',
  'MACDStrategy': 'MACD指标 (MACD)',
  'bollinger': '布林带 (BOLL)',
  'BollingerStrategy': '布林带 (BOLL)'
}

const formatStrategyName = (name) => {
  return strategyNameMap[name] || name
}
```

**修改文件：**
- `backend/strategies/ma_cross_strategy.py`
- `backend/strategies/rsi_strategy.py`
- `backend/strategies/macd_strategy.py`
- `backend/strategies/bollinger_strategy.py`
- `frontend/web/src/pages/Backtest.vue`

---

### FAQ-027: 回测结果需显示交易买卖明细

**发生时间：** 2026-05-19 23:56
**问题类型：** 功能增强
**影响范围：** 回测结果展示

---

#### 需求描述

在回测结果页面添加交易明细表格，显示每笔买入和卖出的详细信息。

---

#### 解决方案

**1. 后端 API 返回交易明细：**

```python
return {
    ...
    "trades": [
        {
            "date": t.date,
            "action": t.action,
            "price": round(t.price, 2),
            "quantity": t.quantity,
            "amount": round(t.total_amount, 2),
            "commission": round(t.commission, 2),
            "slippage": round(t.slippage, 2)
        }
        for t in result.trades
    ]
}
```

**2. 前端添加交易明细表格：**

```vue
<el-card v-if="result.trades?.length" class="mb-16">
  <template #header>
    <div class="card-header">
      <span class="card-title">交易明细</span>
      <span class="card-subtitle">共 {{ result.trades.length }} 笔交易</span>
    </div>
  </template>
  <el-table :data="result.trades" stripe size="small" max-height="300">
    <el-table-column prop="date" label="日期" width="110" />
    <el-table-column label="操作" width="80">
      <template #default="{ row }">
        <el-tag :type="row.action === 'buy' ? 'success' : 'danger'" size="small">
          {{ row.action === 'buy' ? '买入' : '卖出' }}
        </el-tag>
      </template>
    </el-table-column>
    <el-table-column prop="price" label="价格" width="100" align="right" />
    <el-table-column prop="quantity" label="数量" width="100" align="right" />
    <el-table-column label="金额" width="120" align="right" />
    <el-table-column label="手续费" width="100" align="right" />
    <el-table-column label="滑点" width="100" align="right" />
  </el-table>
</el-card>
```

**修改文件：**
- `backend/api/backtest.py`
- `frontend/web/src/pages/Backtest.vue`

---

## 技术要点总结

### 1. API 设计规范

- FastAPI 推荐使用 Pydantic BaseModel 接收 JSON 请求体，而非 Query 参数
- 保持 API 响应结构一致性，包含必要的数据字段

### 2. pandas DataFrame 索引注意事项

- `reset_index(drop=True)` 会将原索引转换为列，同时重置索引为整数
- 访问日期数据时，应从原始 `date` 列获取，而非依赖索引

### 3. 前端参数传递

- Axios 的 params 选项期望对象格式
- 避免传递原始类型（如数字）作为参数

### 4. 数据可视化

- ECharts 图表需要足够的数据点才能正常显示
- 权益曲线应在每一步都记录，而非仅在状态变化时记录

---

## 修改文件清单

| 文件路径 | 修改类型 |
|----------|----------|
| `backend/api/backtest.py` | 修改 |
| `backend/engine/backtest_engine.py` | 修改 |
| `backend/strategies/ma_cross_strategy.py` | 修改 |
| `backend/strategies/rsi_strategy.py` | 修改 |
| `backend/strategies/macd_strategy.py` | 修改 |
| `backend/strategies/bollinger_strategy.py` | 修改 |
| `frontend/web/src/pages/Backtest.vue` | 修改 |

---

> 📝 文档更新日期：2026-05-19 23:56
